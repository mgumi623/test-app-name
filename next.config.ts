// next.config.ts 例（最小構成）
/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: { appDir: true }, ← これを削除
  srcDir: 'src',             // src/ に置いているなら残してOK
};

export default nextConfig;
